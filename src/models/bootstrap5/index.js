import { default as table } from './Table.vue';
import { default as pagination } from './Pagination.vue';
import { default as detail } from './Detail.vue';

import { default as button } from './Button.vue';

import * as formFields from './fields';

export default {
  table,
  pagination,
  detail,
  button,
  formFields,
};
